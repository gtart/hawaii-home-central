# P0 Issues -- Broken

### ID: PS-001
**Summary:** Change detail page returns 404
**Platform:** both
**Tool:** Plan & Changes (project_summary)
**Priority:** P0
**Status:** resolved (2026-03-12)

**Steps to reproduce:**
1. Open a Plan & Changes collection
2. Add a change
3. Click "Details ->" link on the change row

**Expected:** Change detail page renders at `/app/tools/project-summary/[collectionId]/change/[changeId]`
**Actual:** Was returning 404 — stale Next.js dev server cache.

**Root cause:** Stale `.next` cache. Route files exist at correct path.
**Resolution:** Resolved during Plan & Changes v2 migration. ChangeDetailContent.tsx was rewritten with v2 fields. Verified via `npm run build` — route compiles correctly. Pending production deploy verification.
