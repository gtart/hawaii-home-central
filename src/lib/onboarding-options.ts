import { STAGE_PICKER_OPTIONS } from './stage-tool-priority'

/**
 * Re-export stage picker options as onboarding options.
 * The onboarding modal now uses renovation stages instead of intent-based options.
 */
export const ONBOARDING_OPTIONS = STAGE_PICKER_OPTIONS
