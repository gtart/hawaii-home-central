'use client'

import { ToolContent } from '../ToolContent'

export function ProjectSummaryCollectionContent({ collectionId }: { collectionId: string }) {
  return <ToolContent collectionId={collectionId} />
}
